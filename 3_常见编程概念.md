# 常见编程概念

本章都是基础语法,做成 **Rust / Go 双语法速查**,只对真正有差异的点加一句说明。

## 变量与常量

| 项目           | Rust                        | Go                                     |
| -------------- | --------------------------- | -------------------------------------- |
| 声明(不可变) | `let x = 5;`                | 无对应(Go 变量都可变)                |
| 声明(可变)   | `let mut x = 5;`            | `x := 5` / `var x = 5`                 |
| 显式类型       | `let x: i32 = 5;`           | `var x int32 = 5`                      |
| 常量           | `const MAX: u32 = 100_000;` | `const Max = 100_000`                  |
| 遮蔽(shadowing)| `let x = 5; let x = x + 1;` | 同作用域 ❌(重复 `:=` 报错);内层作用域可以 |

- ⚠️ 默认可变性**和 Go 反过来**:Rust 不可变是默认,可变要显式 `mut`
- `const` 必须标类型,只能是编译期常量表达式,可在任何作用域(含全局)声明;命名惯例 `SCREAMING_SNAKE_CASE`
- 遮蔽和 `mut` 的区别:遮蔽是**新变量**,可以改类型(`let spaces = "   "; let spaces = spaces.len();` ✅);`mut` 改值不能改类型

## 数据类型

### 标量类型

| 类别       | Rust                              | Go                                  |
| ---------- | --------------------------------- | ----------------------------------- |
| 有符号整数 | `i8` `i16` `i32` `i64` `i128` `isize` | `int8` `int16` `int32` `int64` `int` |
| 无符号整数 | `u8` `u16` `u32` `u64` `u128` `usize` | `uint8` ... `uint64` `uint` `uintptr` |
| 整数默认   | `i32`                             | `int`(64 位平台 8 字节)           |
| 浮点       | `f32` `f64`(默认 `f64`)         | `float32` `float64`                 |
| 布尔       | `bool`                            | `bool`                              |
| 字符       | `char`(4 字节,Unicode 标量值)  | `rune`(`int32` 别名)              |
| 字节       | `u8`(字面值 `b'A'`)             | `byte`(`uint8` 别名)              |

### 数字字面值

| 写法       | Rust             | Go               |
| ---------- | ---------------- | ---------------- |
| 下划线分隔 | `98_222`         | `98_222`         |
| 十六进制   | `0xff`           | `0xff`           |
| 八进制     | `0o77`           | `0o77`           |
| 二进制     | `0b1111_0000`    | `0b1111_0000`    |
| 类型后缀   | `57u8` `1_000i64`| 无               |

- ⚠️ **溢出行为**:Rust debug 构建 panic,release 构建二进制补码回绕;要明确语义用 `wrapping_*` / `checked_*` / `saturating_*` / `overflowing_*` 方法。Go 永远静默回绕
- 无任何隐式数值转换,`i32 + i64` 直接编译错误(Go 同样要显式转换,这点一致)
- 整数除法向零取整(同 Go)

### 元组(Go 没有)

```rust
let tup: (i32, f64, u8) = (500, 6.4, 1);
let (x, y, z) = tup;    // 解构,≈ Go 的多重赋值
let first = tup.0;      // 按索引访问
```

- Go 最接近的东西是多返回值 + 多重赋值,但元组是真类型,能存能传
- 空元组 `()` 叫 **unit**,表示"无值"——函数不写返回类型时实际返回它(≈ Go 函数无返回值)

### 数组

```rust
let a: [i32; 5] = [1, 2, 3, 4, 5];   // 长度是类型的一部分,栈上分配
let b = [3; 5];                      // [3, 3, 3, 3, 3](Go 没有这种写法)
```

- 与 Go 数组语义基本一致:`[5]int{1, 2, 3, 4, 5}`,长度属于类型、定长、值语义;越界都是运行时 panic
- 变长用 `Vec<T>`(第八章),对应 Go 的 slice

## 函数

```rust
fn add(x: i32, y: i32) -> i32 {
    x + y                // 尾表达式即返回值,不带分号
}
```

```go
func add(x, y int32) int32 {
    return x + y
}
```

- 命名 snake_case;定义顺序无关,调用可在定义之前(同 Go)
- 参数类型逐个标注,不能像 Go 那样 `x, y int32` 合并
- ⚠️ **语句 vs 表达式**是本章唯一的新概念:Rust 里几乎一切是表达式——代码块 `{}` 有值、`if` 有值、函数体最后一个表达式就是返回值。**尾表达式加了分号就变成语句**,返回 `()`,报类型不匹配——新手第一坑
- `return` 关键字存在,用于提前返回;尾返回不写 `return` 是惯用风格

## 注释

- `//` 行注释、`/* */` 块注释,与 Go 相同;文档注释 `///`(对应 Go 的 doc comment)后续章节讲

## 控制流

### if

```rust
if n < 5 {
    ...
} else if n < 10 {
    ...
} else {
    ...
}
```

- 条件不带括号(同 Go);条件**必须是 `bool`**(同 Go,不像 C/JS 有 truthy)
- ⚠️ if 是**表达式**,替代了三元运算符(Go 连三元都没有,只能写完整 if):

```rust
let number = if condition { 5 } else { 6 };   // 各分支类型必须一致
```

### 循环

| 需求       | Rust                              | Go                                        |
| ---------- | --------------------------------- | ----------------------------------------- |
| 无限循环   | `loop { }`                        | `for { }`                                 |
| 条件循环   | `while cond { }`                  | `for cond { }`                            |
| 计数循环   | `for i in 0..10 { }`              | `for i := 0; i < 10; i++` / `for i := range 10` |
| 遍历集合   | `for x in &arr { }`               | `for _, x := range arr { }`               |
| 倒序       | `for i in (1..4).rev() { }`       | 手写三段式                                |
| 循环返回值 | `let r = loop { break val; };`    | 无                                        |
| 嵌套跳出   | `'outer: loop { break 'outer; }`  | `outer: for { break outer }`              |

- range 语法:`0..10` 半开区间,`0..=10` 闭区间
- `loop` + `break 值` 是"重试直到成功并带出结果"的惯用写法,Go 得在循环外声明变量
- ⚠️ 没有 `++` / `--`,写 `n += 1`(Go 的 `n++` 好歹是个语句,Rust 干脆没有)

## 速查之外的差异雷达(TL;DR)

1. ⚠️ 可变性默认值与 Go 相反:不加 `mut` 不能改
2. ⚠️ 尾表达式加分号 = 返回 `()`,类型不匹配报错先检查分号
3. ⚠️ 整数溢出 debug panic、release 回绕,行为不一致;敏感场景显式用 `checked_*` 系列
4. ✅ 遮蔽是新变量、能改类型,处理"解析转换链"很顺手,不算坏味道
5. ✅ `if`/`loop`/块都是表达式,少量状态变量可以直接用表达式赋值消掉
