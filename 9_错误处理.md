# 错误处理

一句话总结:Rust **没有异常**。错误分两类——可恢复错误用返回值 `Result<T, E>` 表达,不可恢复错误用 `panic!` 中止程序;所有"可能失败"都写在函数签名里,编译器强迫你在写代码时就面对它。

## 与 Java/Go 的模型对照

| 场景             | Rust                                   | Java                            | Go                                |
| ---------------- | -------------------------------------- | ------------------------------- | --------------------------------- |
| 可恢复错误       | `Result<T, E>` 返回值                  | 异常 `throw`(受检/非受检)     | `error` 返回值                    |
| 不可恢复错误     | `panic!`                               | `Error` / `RuntimeException`    | `panic`                           |
| 错误传播         | `?` 运算符                             | 异常自动向上冒泡                | `if err != nil { return err }`    |
| 能否静默忽略     | 难:不用 Result 有编译警告,取值必须处理 | 非受检异常可无声穿透所有层     | 易:`_` 一扔就没了               |
| 捕获/恢复        | 一般不捕获(`catch_unwind` 极少用)    | `try/catch`                     | `recover`                         |

- Rust 的模型和 **Go 几乎同构**(错误值 + panic 双轨制),但 `?` 消灭了 `if err != nil` 样板代码,且 `Result` 标注了 `#[must_use]`——拿到不处理,编译器警告
- 与 **Java** 的根本差异:没有异常的自动栈展开传播,可失败性全部显式写在返回类型里。比受检异常更彻底——受检异常还有 `RuntimeException` 这个"后门",Rust 没有

## 不可恢复错误:`panic!`

### panic 的两种来源

```rust
panic!("crash and burn");   // 1. 显式调用

let v = vec![1, 2, 3];
v[99];                      // 2. 执行了坏操作(越界)
```

- ⚠️ 越界是 panic 而不是像 C 那样读到脏内存——这是安全性保证,不是缺陷

### 展开(unwinding)vs 终止(abort)

- 默认**展开**:回溯栈、逐帧清理数据后退出,代价是二进制里要带清理代码
- 可配置为**终止**:立即退出,内存回收交给操作系统,二进制更小:

```toml
[profile.release]
panic = 'abort'
```

### backtrace

```bash
RUST_BACKTRACE=1 cargo run      # 基础调用栈
RUST_BACKTRACE=full cargo run   # 完整版
```

- 需要 debug symbols(不加 `--release` 时默认开启)
- 阅读方法:**从上往下,找到第一个自己写的文件**,问题就从那里开始(和读 Java 异常堆栈同理)

## 可恢复错误:`Result<T, E>`

### 定义

```rust
enum Result<T, E> {
    Ok(T),
    Err(E),
}
```

- 就是第 6 章的普通枚举,没有任何魔法;在 prelude 里,`Ok`/`Err` 不用加前缀直接写
- `File::open` 返回 `Result<File, io::Error>`——失败的可能性和错误类型**写在签名里**,不像 Java 要翻文档看 throws、Go 要看 error 的动态类型

### match 处理与区分错误种类

```rust
use std::fs::File;
use std::io::ErrorKind;

let greeting_file = match File::open("hello.txt") {
    Ok(file) => file,
    Err(error) => match error.kind() {              // io::ErrorKind 区分错误种类
        ErrorKind::NotFound => match File::create("hello.txt") {
            Ok(fc) => fc,
            Err(e) => panic!("创建失败: {e:?}"),
        },
        _ => panic!("打开失败: {error:?}"),
    },
};
```

- `error.kind()` ≈ Java 里 catch 不同异常子类、Go 里 `errors.Is/As` 的角色
- 嵌套 match 很啰嗦,惯用写法是闭包 + `unwrap_or_else`(第 13 章),先见过不慌

### `unwrap` 与 `expect`

```rust
let f = File::open("hello.txt").unwrap();    // Err 就 panic,消息是 Debug 格式的错误
let f = File::open("hello.txt")
    .expect("hello.txt 应该随项目一起分发");  // 同上,但 panic 消息自定义
```

- 生产代码倾向用 `expect`,消息写清**为什么此处应该成功**,排查时比 unwrap 的默认消息有用得多
- 类比:Java `Optional.get()`、Go 社区的 `Must*` 系列函数

### 传播错误与 `?` 运算符 ⭐

手写传播(即 Go 程序员每天写的东西):

```rust
fn read_username_from_file() -> Result<String, io::Error> {
    let username_file_result = File::open("hello.txt");
    let mut username_file = match username_file_result {
        Ok(file) => file,
        Err(e) => return Err(e),          // 失败就提前返回给调用者
    };
    let mut username = String::new();
    match username_file.read_to_string(&mut username) {
        Ok(_) => Ok(username),
        Err(e) => Err(e),
    }
}
```

`?` 版,语义完全相同:

```rust
fn read_username_from_file() -> Result<String, io::Error> {
    let mut username = String::new();
    File::open("hello.txt")?.read_to_string(&mut username)?;   // 还能链式
    Ok(username)
}
```

- `?` 的行为:值是 `Ok` 就**取出内部值**继续执行;是 `Err` 就**提前 return** 整个函数——把 Go 的三行样板压缩成一个字符
- ⚠️ `?` 与手写 match 有一处不同:错误会先经过 **`From::from` 自动转换**——只要实现了 `impl From<ErrA> for ErrB`,函数里冒出的 `ErrA` 就能自动转成签名声明的 `ErrB`。这是"自定义统一错误类型"的机制基础(类比 Java 里手动 wrap 异常,这里全自动)
- 这个场景标准库其实一步到位:`fs::read_to_string("hello.txt")`——常见组合都封装好了

### ⚠️ `?` 的使用限制

- `?` 只能用在**返回类型兼容**的函数里:函数返回 `Result` 才能对 Result 用 `?`;返回 `Option` 才能对 Option 用 `?`(`None` 时提前返回 `None`)
- 两者**不能混用**:返回 Option 的函数里不能对 Result 用 `?`,反之亦然。要转换:`res.ok()`(Result→Option)、`opt.ok_or(err)`(Option→Result)
- 普通 `main() -> ()` 里不能用 `?`,但 main 可以改签名:

```rust
use std::error::Error;

fn main() -> Result<(), Box<dyn Error>> {
    let greeting_file = File::open("hello.txt")?;
    Ok(())
}
```

- `Box<dyn Error>` 暂时理解为"任何错误"的万能容器(trait 对象,第 18 章)
- main 返回 `Err` 时进程以非 0 退出码结束——shell 脚本友好,不用手写 `process::exit`

## 何时 panic,何时 Result

默认选 **Result**——把处理权交给调用者,panic 是替所有调用者做了"必死"的决定。可以 panic 的场景:

1. **示例 / 原型 / 测试**:`unwrap`/`expect` 当占位符;测试里 panic 正是标记失败的机制
2. **你比编译器知道得多**:逻辑上不可能失败,但类型上仍是 Result:

```rust
let home: IpAddr = "127.0.0.1"
    .parse()
    .expect("硬编码的 IP 地址不可能非法");
```

   expect 消息写"为什么不可能失败",将来假设变了(比如 IP 改成用户输入)也好定位

3. **坏状态(bad state)**:假设、约定、不变量被破坏(非法值、矛盾状态),且同时满足:非预期情况、后续代码依赖不处于此状态、无法用类型编码。一句话:**契约被违反 = 调用方的 bug → panic**(类比 Java 抛 `IllegalArgumentException`、Go 对编程错误 panic 的惯例)
4. **预期中会发生的失败**(解析格式烂的外部数据、HTTP 限流、文件不存在)→ 老老实实 Result

### 用类型系统消灭重复校验 ⭐

```rust
pub struct Guess {
    value: i32,           // 字段私有!(第 7 章:pub struct 字段默认私有)
}

impl Guess {
    pub fn new(value: i32) -> Guess {
        if value < 1 || value > 100 {
            panic!("Guess 必须在 1 到 100 之间,得到 {value}。");
        }
        Guess { value }
    }

    pub fn value(&self) -> i32 {   // getter,借用 self
        self.value
    }
}
```

- 字段私有 + 唯一入口 `new` 里校验 → **拿到 `Guess` 实例就保证合法**;下游函数签名直接收 `Guess`,永远不用重复校验
- 这就是"让非法状态不可表示"(parse, don't validate)思想;类比 Java 的值对象(Value Object),但 Rust 靠私有性硬性保证,没有反射/序列化后门

## Java/Go 程序员避坑清单(TL;DR)

1. ❌ 没有 try/catch,别找了。可恢复走 `Result`,不可恢复走 `panic!`,双轨泾渭分明
2. ❌ 拿到 `Result` 不处理会有编译警告(`#[must_use]`),不像 Go 的 error 能悄悄 `_` 掉
3. ❌ `?` 不能用在返回 `()` 的 main 里;改签名 `fn main() -> Result<(), Box<dyn Error>>`
4. ❌ Result 的 `?` 和 Option 的 `?` 不能混用,用 `ok()` / `ok_or()` 桥接
5. ✅ `?` 会经过 `From` 自动转换错误类型——统一错误类型靠 `impl From` 而不是到处手动 wrap
6. ✅ 生产代码用 `expect` 替代 `unwrap`,消息写"为什么此处应该成功"
7. ✅ 库代码默认返回 Result 交出决定权;unwrap 留给应用的最外层边界
8. ✅ 判断标准:调用方违反契约(bug)→ panic;环境性/预期内失败(IO、外部数据)→ Result
9. ✅ 校验逻辑收进 newtype(私有字段 + 构造校验),让下游拿到即合法
