# 常见集合

一句话总结:三种最常用的标准库集合——`Vec<T>`(动态数组)、`String`(UTF-8 字符串)、`HashMap<K, V>`(哈希表),数据都在**堆**上,大小可动态变化;与 Java/Go 最大的不同是**所有权和借用规则贯穿一切操作**。

## Vector(`Vec<T>`)

### 创建与更新

```rust
let v: Vec<i32> = Vec::new();   // 空 vector 需要类型注解,编译器无从推断
let v = vec![1, 2, 3];          // vec! 宏,自动推断为 Vec<i32>,更常用

let mut v = Vec::new();         // 这里不用注解:后面 push 了 i32,编译器能推断
v.push(5);                      // 修改必须 mut(Java/Go 没有这层区分)
```

### 读取:索引 vs `get` ⭐

| 方式        | 返回值         | 越界行为                 | 适用场景                     |
| ----------- | -------------- | ------------------------ | ---------------------------- |
| `&v[2]`     | `&T`           | **panic**                | 越界即 bug,程序就该崩       |
| `v.get(2)`  | `Option<&T>`   | 返回 `None`              | 越界是正常情况(如用户输入) |

Java 里只有"越界抛异常"一种;Go 里只有越界 panic。Rust 把选择权交给你,且 `get` 用类型强迫你处理 `None` 分支。

### ⚠️ 借用检查器:拿着元素引用时不能 push

```rust
let mut v = vec![1, 2, 3, 4, 5];
let first = &v[0];        // 不可变借用开始
v.push(6);                // ❌ 编译错误:push 需要可变借用
println!("{first}");      // 不可变借用还活着(最后一次使用在这)
```

- 原因:`push` 可能触发**扩容搬迁**(和 `ArrayList` 扩容、Go slice 的 `append` 重新分配同理),旧引用会悬垂。Java 靠 GC 兜底、Go 的旧 slice 还指向旧数组"静默不同步"——Rust 直接**编译期拒绝**
- 类比:Java 迭代中修改集合抛 `ConcurrentModificationException` 是**运行时**才炸;Rust 在编译期就拦住,且不限于迭代场景

### 遍历

```rust
for i in &v { println!("{i}"); }      // 不可变遍历
for i in &mut v { *i += 50; }         // 可变遍历,注意要解引用 *
```

- 遍历期间增删元素同样被借用检查器禁止(整个 for 持有对 v 的借用)

### 用枚举存"多种类型"

vector 只能存同一类型;需要异构时用枚举包一层(Java 用 `Object`/多态,Go 用 `interface{}`/`any`,Rust 用 enum——**编译期就知道所有可能的类型**,match 时必须穷尽处理):

```rust
enum SpreadsheetCell {
    Int(i32),
    Float(f64),
    Text(String),
}

let row = vec![
    SpreadsheetCell::Int(3),
    SpreadsheetCell::Text(String::from("blue")),
    SpreadsheetCell::Float(10.12),
];
```

- 类型集合编译期未知时才需要 trait 对象(第 18 章)

### 内存

- vector 离开作用域时,连同**所有元素**一起被 drop,无需手动清理(无 GC 但也无 `free`)

## 字符串(`String`)

### String vs &str

- `&str`:核心语言唯一的字符串类型,是对别处 UTF-8 数据的**借用**;字面值 `"hello"` 就是 `&str`
- `String`:标准库提供,可增长、可变、**拥有所有权**,同样保证 UTF-8
- `String` 实际上就是 `Vec<u8>` 的封装 + UTF-8 约束,所以很多 Vec 的心智模型可以复用

### 创建与更新

```rust
let mut s = String::new();
let s = "initial contents".to_string();   // 与下面等价,风格问题
let s = String::from("initial contents");

let mut s1 = String::from("foo");
s1.push_str("bar");    // 追加 &str,不夺取参数所有权
s1.push('l');          // 追加单个 char,注意单引号
```

### ⚠️ `+` 拼接会移动所有权

```rust
let s1 = String::from("Hello, ");
let s2 = String::from("world!");
let s3 = s1 + &s2;     // s1 被移动,之后 ❌ 不能再用 s1;s2 仍可用
```

- 签名近似 `fn add(self, s: &str) -> String`:第一个操作数**按值拿走**,第二个只是借用
- `&s2` 是 `&String`,靠 **deref 强制转换**自动变成 `&str`
- Java 的 `+` 每次都新建对象、原字符串还在;Rust 是复用 s1 的缓冲区再追加,性能更好但 s1 没了
- 多段拼接用 `format!` 宏(类比 `String.format` / `fmt.Sprintf`),**不夺取任何参数所有权**:

```rust
let s = format!("{s1}-{s2}-{s3}");
```

### ⚠️ 不能索引:`s[0]` 编译错误 ⭐

原因有三:

1. UTF-8 变长编码,`s[0]` 拿到的字节大概率不是一个完整"字符"
2. "字符"本身有歧义(见下面三层结构),Rust 拒绝替你选一个错的默认
3. 索引应当是 O(1),但在 UTF-8 里定位第 n 个字符必须 O(n) 扫描,Rust 不愿提供带欺骗性的 API

对照:

- **Go 程序员会觉得熟悉**:Go 的 `s[0]` 返回的也是字节(byte),`range` 遍历才按 rune——Rust 只是更激进,连返回字节的索引都不给(详见下面「对比 Go 的 string」)
- **Java 的坑更隐蔽**:`charAt(0)` 返回 UTF-16 code unit,遇到 emoji 等增补字符拿到的是半个代理对——Java 其实有同样的问题,只是 API 假装没有(详见下面「对比 Java 的 UTF-16」)

### 背景:Unicode 字符集与 UTF 编码

- **字符集与编码是两件事**:Unicode(字符集)给每个字符分配一个**码点**(code point),写作 `U+0417` 这样,范围 `U+0000`..`U+10FFFF`;UTF-8/16/32 则是把码点转成字节的三种**编码方式**
- 三种编码方式对比:

| 编码   | 长度        | 特点                                     | 典型使用者                              |
| ------ | ----------- | ---------------------------------------- | --------------------------------------- |
| UTF-32 | 定长 4 字节 | 码点原样存,可 O(1) 索引,但空间浪费大   | Rust 的 `char`(单字符就是个 UTF-32 值)|
| UTF-16 | 2 或 4 字节 | BMP 内 2 字节,其余用**代理对**凑 4 字节 | Java / C# / JavaScript 的内部表示       |
| UTF-8  | 1~4 字节    | 变长,兼容 ASCII,网络与存储的事实标准   | Rust `String`、Go `string`              |

- **UTF-8 变长编码如何确定字节数**——看首字节的前导位:

| 码点范围              | 字节数 | 首字节     | 后续字节      |
| --------------------- | ------ | ---------- | ------------- |
| `U+0000`..`U+007F`    | 1      | `0xxxxxxx` | —(即 ASCII)|
| `U+0080`..`U+07FF`    | 2      | `110xxxxx` | `10xxxxxx`    |
| `U+0800`..`U+FFFF`    | 3      | `1110xxxx` | `10xxxxxx` ×2 |
| `U+10000`..`U+10FFFF` | 4      | `11110xxx` | `10xxxxxx` ×3 |

- 规则一句话:**首字节开头连续 1 的个数 = 该字符总字节数**(`0` 开头 = 单字节 ASCII);后续字节一律 `10` 开头
- 关键推论:任取一个字节就能判断它是否是字符边界(不是 `10xxxxxx` 开头就是边界),**无需从头扫描**——这就是 Rust 能 O(1) 检查 slice 是否切在字符中间、panic 消息能精确指出 "inside 'З' (bytes 0..2)" 的原因;也是 UTF-8 自同步(损坏一个字节只影响一个字符)的原因
- 手工验证:'З' 码点 `U+0417` = 二进制 `100_0001_0111`(11 位),套 2 字节模板得 `110_10000` `10_010111` = `[208, 151]`——这就是下面 slice 例子里每个西里尔字母占 2 字节的由来

### 对比 Java 的 UTF-16

- Java 的 `char` 是 16 位 **UTF-16 code unit**,不是"字符"。历史包袱:Java 诞生时 Unicode 还是 16 位定长(设计意图就是定长省心),后来 Unicode 扩到 `U+10FFFF`,BMP 之外的字符只能用**代理对**(高位 `U+D800`..`U+DBFF` + 低位 `U+DC00`..`U+DFFF`,两个 char)拼出来——UTF-16 从此也变成了变长编码
- 后果:`"😀".length() == 2`、`charAt(0)` 拿到半个代理对、`substring` 可能把代理对切开产生烂字符串——本质上和 Rust 面对的是同一个问题,但 Java 的 API 维持着"定长"的假象,坑留到运行时才炸
- 正确处理 BMP 外字符得用 `codePointAt` / `codePoints()`,实际上大多数 Java 代码从没这么写
- Rust 的 `char` 是完整的 **Unicode 标量值**(= 码点去掉代理对区段,4 字节),`.chars()` 永远不会吐出半个字符——"标量值"这个词在书里反复出现,指的就是这个
- 空间对比:ASCII 用 UTF-8 是 1 字节、UTF-16 是 2 字节;中文用 UTF-8 是 3 字节、UTF-16 是 2 字节——没有全赢的方案,Java 9+ 还为此搞了 Compact Strings(纯 Latin-1 内容按 1 字节存)

### 对比 Go 的 string

Go 的字符串**能**索引——但 `s[i]` 拿到的是**字节**(`byte` 即 `uint8`),不是字符:

```go
s := "Здравствуйте"
b := s[0]                      // 208 —— 只是 'З' 的第一个字节
sub := s[0:1]                  // 切在字符中间,不报错,打印出 "�"
for i, r := range s { ... }    // range 才按 rune 解码,i 是【字节】偏移,会跳着走
```

Go 和 Rust 同属 UTF-8 阵营,内存布局几乎一样,差别在**保证的强度**:

| 行为         | Go `string`                              | Rust `String` / `&str`                     |
| ------------ | ---------------------------------------- | ------------------------------------------ |
| 按索引取     | `s[0]` 返回字节,O(1)                    | ❌ 编译错误,想要字节得显式 `as_bytes()[0]` |
| 按字节切     | `s[0:1]` 静默产生无效 UTF-8              | `&s[0..1]` 非字符边界直接 panic            |
| 内容保证     | **不保证**合法 UTF-8(可存任意字节)     | 构造时即保证合法 UTF-8                     |
| 按字符遍历   | `for _, r := range s`(非法字节给 `U+FFFD`)| `s.chars()`(不可能遇到非法字节)         |
| 字符数       | `utf8.RuneCountInString(s)`              | `s.chars().count()`                        |
| 随机访问字符 | `[]rune(s)[i]`(O(n) 转换)              | `s.chars().nth(i)`(同样 O(n),不装了)    |

- `rune` 只是 `int32` 的**别名**,塞什么都行;Rust 的 `char` 是类型系统保证的合法 Unicode 标量值——`u32` 转 `char` 必须走 `char::from_u32` 校验,返回 `Option`
- 一句话总结三家路线:**Java 假装定长(坑延迟到运行时)、Go 暴露字节(烂数据静默流过)、Rust 拒绝歧义(编译期或边界检查处就拦下)**

### 内部表示的三层结构

以印地语 "नमस्ते" 为例:

| 层次                  | 内容                               | 数量 | 对应 API                 |
| --------------------- | ---------------------------------- | ---- | ------------------------ |
| 字节(`u8`)          | `[224, 164, 168, ...]`             | 18   | `.bytes()`               |
| Unicode 标量值(`char`)| `['न', 'म', 'स', '्', 'त', 'े']`   | 6(含 2 个不独立成字的变音符号) | `.chars()` |
| 字形簇("人眼字母") | `["न", "म", "स्", "ते"]`            | 4    | 标准库不提供,需第三方 crate |

### slice 与遍历

```rust
let hello = "Здравствуйте";      // 西里尔字母,每个占 2 字节
let s = &hello[0..4];            // "Зд" —— range 按【字节】计
// &hello[0..1]                  // ❌ 运行时 panic:切在字符中间
```

- 遍历必须先想清楚要哪一层:`for c in s.chars()` 按字符,`for b in s.bytes()` 按字节
- 常用操作标准库都有:`contains`、`replace` 等,别自己按字节手搓

## Hash Map(`HashMap<K, V>`)

### 基本使用

```rust
use std::collections::HashMap;   // ⚠️ 不在 prelude 中,必须显式引入

let mut scores = HashMap::new();
scores.insert(String::from("Blue"), 10);
scores.insert(String::from("Yellow"), 50);

let team_name = String::from("Blue");
let score = scores.get(&team_name).copied().unwrap_or(0);
// get 返回 Option<&V> → copied() 变 Option<V> → unwrap_or 给默认值
// 整条链 ≈ Go 的 value, ok := m[k] + if !ok 兜底,但一行写完

for (key, value) in &scores {    // 遍历顺序不确定(Go map 同;Java HashMap 同)
    println!("{key}: {value}");
}
```

### ⚠️ insert 会移动所有权

```rust
let field_name = String::from("Favorite color");
let field_value = String::from("Blue");
let mut map = HashMap::new();
map.insert(field_name, field_value);
// ❌ field_name / field_value 已被移动,之后不能再用
```

- `i32` 等 Copy 类型是复制进去;`String` 等所有权类型被**移动**,map 成为新所有者
- Java/Go 里放进 map 的是引用/值的拷贝,原变量照用不误——Rust 这里是本章最容易踩的坑
- 插入引用(`&T`)也行,但引用必须活得比 map 久(生命周期,第 10 章)

### 更新的三种姿势 ⭐

```rust
// 1. 覆盖:同键再 insert 直接替换旧值
scores.insert(String::from("Blue"), 10);
scores.insert(String::from("Blue"), 25);   // 现在是 25

// 2. 不存在才插入:entry + or_insert(≈ Java 的 computeIfAbsent)
scores.entry(String::from("Yellow")).or_insert(50);

// 3. 基于旧值更新:or_insert 返回 &mut V,原地改
let text = "hello world wonderful world";
let mut map = HashMap::new();
for word in text.split_whitespace() {
    let count = map.entry(word).or_insert(0);
    *count += 1;                           // 注意解引用
}
```

- `entry` API 是惯用法,比"先 get 判断再 insert"查一次哈希就够,也绕开了借用冲突
- Go 没有对应物,只能 `if _, ok := m[k]; !ok { ... }` 手写;Java 8 的 `merge`/`computeIfAbsent` 最接近

### 哈希函数

- 默认 **SipHash**:抗哈希碰撞 DoS 攻击,代价是不是最快的
- 性能敏感时可换 hasher(实现 `BuildHasher` trait,crates.io 上有现成的,如 `fxhash`)
- 对照:Java 8+ 用链表树化对抗碰撞攻击;Go map 用随机种子——三家都防,手段不同

## Java/Go 程序员避坑清单(TL;DR)

1. ❌ 拿着 vector 元素的引用时不能 `push`——扩容会使引用悬垂,编译期直接拒绝,别跟借用检查器讲道理
2. ❌ `String` 不能 `s[0]` 索引;想按字节/字符/字形簇处理,分别用 `bytes()` / `chars()` / 第三方 crate
3. ❌ 字符串 slice `&s[0..n]` 按**字节**切,切在字符中间会 panic(Go 是静默产生烂数据,Java 是半个代理对)
4. ❌ `s1 + &s2` 会移动 s1;多段拼接用 `format!`,谁的所有权都不动
5. ❌ `insert` 会把 `String` 等非 Copy 键值的所有权**移进** map,原变量作废
6. ❌ `HashMap` 不在 prelude,忘了 `use std::collections::HashMap` 编译器会提醒你
7. ✅ 越界可能是正常输入就用 `v.get(i)` 处理 `Option`;越界即 bug 就用 `v[i]` 让它崩
8. ✅ "不存在才插入/累加计数"用 `entry(k).or_insert(v)`,别手写 get + insert 两连
9. ✅ 异构集合优先考虑 enum 包装(编译期穷尽检查),而不是急着上 trait 对象
